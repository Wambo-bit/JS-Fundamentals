#!/usr/bin/node

// Get the two arguments (ignoring "node" and script path)
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// Print them in the required format
console.log(`${arg1} is ${arg2}`);
